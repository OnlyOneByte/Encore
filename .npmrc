# Encore is a personal OnlyOneByte project — pull from PUBLIC npm, not Amazon CodeArtifact.
# This repo-local override keeps personal/corp registries cleanly separated; the global
# ~/.npmrc (CodeArtifact) is left untouched. See docs/MASTER-DESIGN.md.
registry=https://registry.npmjs.org/
